# Development Environment Variables
# This file is used when running the app in development mode (npm run dev)

# Optional explicit backend override for development
# If set, the frontend will call this backend directly and bypass the Vite proxy.
# Example (local): VITE_BACKEND_URL=http://localhost:3010
# Example (Railway): VITE_BACKEND_URL=https://your-app.railway.app
# VITE_BACKEND_URL=

# Development mode flag (automatically set by Vite)
# NODE_ENV=development
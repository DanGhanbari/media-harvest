# Production Environment Variables
# This file is used when building for production (npm run build)

# API Base URL for production (uses Vercel proxy to VPS backend)
# This routes through Vercel's proxy configuration to avoid CORS/mixed content issues
# Using relative URLs to leverage Vercel proxy
# VITE_API_BASE_URL=

# Production mode flag (automatically set by Vite)
# NODE_ENV=production
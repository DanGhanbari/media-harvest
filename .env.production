# Production Environment Variables
# Frontend on Vercel, backend on Railway

# Explicit backend override for production (optional)
# Set to your Railway backend URL if you want the frontend to call it directly
# Otherwise, frontend uses serverless functions which proxy to Railway
VITE_BACKEND_URL=

# Production mode flag (automatically set by Vite)
# NODE_ENV=production